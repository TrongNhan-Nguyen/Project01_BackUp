<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:padding="5dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/dEditComment_tvTitle"
        android:text="EDIT YOUR COMMENT"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_centerHorizontal="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
    <EditText
        android:id="@+id/dEditComment_etComment"
        android:layout_below="@+id/dEditComment_tvTitle"
        android:layout_toLeftOf="@+id/dEditComment_imgPost"
        android:layout_marginRight="5dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
    <ImageView
        android:id="@+id/dEditComment_imgPost"
        android:src="@drawable/ic_send_black_24dp"
        android:layout_alignParentRight="true"
        android:layout_alignBottom="@+id/dEditComment_etComment"
        android:layout_width="40dp"
        android:layout_height="40dp"/>

</RelativeLayout>